# CMCHT-XAI: Cross-Modal Contrastive Hybrid Transformer with Counterfactual XAI
# Python 3.12 + Windows (tested clean install)
# -----------------------------------------------------------------------
# KEY FIX: rtdl 0.0.13 hard-pins torch<2 — it is abandoned and removed.
# FT-Transformer is replaced by tab-transformer-pytorch which supports torch 2.x
# and implements the same FT-Transformer architecture from the original paper.
# -----------------------------------------------------------------------

# Core deep learning
torch==2.4.1
torchvision==0.19.1
numpy==1.26.4
pandas==2.2.3
scipy==1.13.1
scikit-learn==1.5.2

# Medical imaging
monai==1.3.2
SimpleITK==2.3.1
nibabel==5.2.1
opencv-python==4.10.0.84
Pillow==10.4.0

# Vision transformer backbones (Swin-CNN encoder)
timm==1.0.9

# Tabular transformer — FT-Transformer (replaces abandoned rtdl which pins torch<2)
# This implements the exact same FT-Transformer from Gorishniy et al. 2021
tab-transformer-pytorch==0.4.2

# Explainability (XAI layer)
scikit-image==0.24.0
shap==0.46.0
captum==0.7.0
dice-ml==0.12
# alibi needs tensorflow which conflicts on Windows — use alibi-detect (lighter)
alibi-detect==0.13.0

# Data balancing
imbalanced-learn==0.12.3

# API (dashboard backend)
fastapi==0.111.0
uvicorn[standard]==0.30.1
python-multipart==0.0.9

# Config & utilities
PyYAML==6.0.1
tqdm==4.66.4
matplotlib==3.9.0
seaborn==0.13.2